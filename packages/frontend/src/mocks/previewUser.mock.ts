import type { User } from '../services/auth.service';

export const mockUser: User = {
  id: 'mock-admin-id-1234',
  nome: 'Administrador Oliveira (Preview)',
  email: 'admin.preview@oliveira.local',
  perfil: 'ADMIN',
};
